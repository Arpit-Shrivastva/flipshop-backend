spring.application.name=FlipShop


# Database URL, username, and password
spring.datasource.url=jdbc:mysql://localhost:3306/flipShop?createDatabaseIfNotExist=true
spring.datasource.username= ****
spring.datasource.password= ****

# Hibernate properties for JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Connection pool settings (optional, depending on requirements)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.initialization-mode=always
